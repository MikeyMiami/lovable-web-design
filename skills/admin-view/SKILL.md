---
name: admin-view
description: Use when building, modifying, or reviewing the admin dashboard on a client website — the agency/admin tabs and the per-client Settings fields. Defines which tabs exist and which configurable values must live in Settings. Use when adding or adjusting what is editable in the admin view. NOT for the client mobile app (use mobile-app).
---

# Admin View — tabs & settings on the client website

The admin dashboard lives at `/admin` on the client website, gated to `admin` / `agency_owner` roles (RLS-scoped). It is where the agency manages this client. Build/maintain these tabs.

**Role model + access gate.** This is an agency-scoped surface (`admin`/`agency_owner` — `is_admin` covers both). It is NEVER payment-gated: the payment-access gate suspends only the client's mobile PWA (`client_owner`/`client_staff` surface), never the agency's per-client admin view or agency account. Client logins are minted by `provisionClientOwner` (scratch-foundation §5), not here.

## Tabs
- **Dashboard** (`/admin`) — KPIs (contacts in/out, sends, reviews) for the active client.
- **Contacts** (`/admin/contacts`) — CRM list + detail.
- **Conversations** (`/admin/conversations`) — full SMS inbox + threads.
- **Feedback** (`/admin/feedback`) — low-star private feedback submissions.
- **Edit Requests** (`/admin/edit-requests`) — **[BUILT — B-design Prompt 3, 2026-06-20]** per active client, the client's **Request-an-Edit** tickets (`kind='edit_request'`). List with a **Pending (default) / All / History** filter; open a thread (bubbles, `sender_side` alignment; attachments via **signed URL** from `client-assets`). **Agency lifecycle actions:** reply (`postAgencyReply` — forces `sender_side='agency'`, optional `in_progress`); **Approve / Deny — each REQUIRES a resolution** (`setTicketStatus` → `approved`/`denied` + resolution, returned to the client in-app); **Close** after implementing. **No auto re-deploy** — approve = notify + record the resolution; the agency implements the edit manually (remix / Settings).
- **Support** (`/admin/support`) — **[BUILT — B-design Prompt 3, 2026-06-20]** per active client, the client's **Support** tickets (`kind='support'`). Same shape; lifecycle = reply → **Mark in progress** → **Resolve** (`setTicketStatus`); resolved threads live under the **History** filter.
  - Both are one shared **`AdminTicketSurface`** component (parameterized by `kind`), mirroring the client PWA's `TicketSurface`. **Security contract [LOCKED]:** admin **reads via the authed `supabase` (admin JWT → `is_admin()` RLS branch) — NEVER service-role in the admin UI**; **all writes go through the role-verified service-role fns** `postAgencyReply` / `setTicketStatus` (which set `sender_side`/`status`/`resolution` authoritatively — the ticket tables are read-only-RLS + write-REVOKEd, so the UI cannot write directly). Notifications + owner-email stub fire automatically from the fns (`notify.server.ts`). **Cross-tenant** pending-edit / pending-support lists across ALL clients = the separate **agency-view** (Phase C), NOT here (this surface is per-active-client). Data model + the write boundary: `scratch-foundation` (tickets) + `mobile-app` (the client side).
- **Automations** (`/admin/automations`) — edit message templates + sequence steps (the editable surface of the automation-config content). Each drip also shows a **live count of contacts currently enrolled** (active) in it — read from `enrollments WHERE status='active'` grouped by `sequence_key`. Gives at-a-glance visibility into what's running per drip. [BUILD — new]
- **Upload Customers** (`/admin/reactivation`) — CSV/paste uploader that enrolls uploaded past customers into the per-client **Customer Review Reactivation drip** (normalize phones → dedupe → enroll, caps 50/day + 2/20min). **[LIVE — rewired 2026-07-21]** the drip is delivered by the NORMAL send runner from the **client's own `clients.telnyx_number`** (`provider='telnyx'`), inheriting the normal throttles (daily send cap, per-client slice, jitter, send-window) plus these enrollment caps; a **REQUIRED consent-attestation checkbox** ("past customers who consented to SMS") gates the upload. (The former agency number pool + its "Number Pool" tab were REMOVED — see below + `/features`.)
- **~~Reactivation Numbers (agency pool)~~ [REMOVED 2026-07-21 — no longer part of the admin surface]** — the agency-owned number-pool surface (its "Number Pool" nav link, the `admin.reactivation-pool.tsx` route, `src/lib/reactivation/{runner,pool.functions,release}.server.ts`, the `/api/public/cron/reactivation` route, and the `reactivation_numbers`/`reactivation_campaigns`/`reactivation_enrollments` tables + `create_reactivation_campaign`/`claim_due_reactivation` RPCs) was DELETED. Reactivation now sends from the client's own Telnyx number via the per-client drip (see **Upload Customers** above + `/features`); the global `reactivation` SEQUENCE remains. Historical rationale: `docs/reactivation-number-pool-spec.md`.
- **A2P Prep (per client)** — pre-generates the business-customized Brand + Campaign copy for copy-paste into TextGrid/Telnyx (the pack feeds both providers — same TCR registry) (Brand fields incl. **legal business address** (`clients.legal_address`, editable in Settings; from IRS docs — may differ from the public address), Campaign Description, Call-to-Action/consent paragraph, 5 sample messages with real values + ≥2 STOP, Privacy/Terms URLs), filled from the one token schema + the per-niche category library, with a **toggle to view the approved Review Harvest example side-by-side.** Source: `/a2p-site-compliance` (+ verbatim copy `docs/a2p-compliance-copy-source-of-truth.md`). [BUILD — new]
- **Pre-generation console / Review & Finalize (per client) [BUILT — C-3c-2b — lean v1 at `/admin/review`]** — the per-client review/finalize surface (active-client scoped): a **status pill** (amber `pending` / green `active`), the **immutable submission viewer** (below), a **missing-required-`template_vars`** reminder, a link to editable **Settings**, and the shared **`<FinalizeInvite/>`** (handoff checklist + invite + pending→active). The richer **congruence console** (style/branding/niche/A2P-prep assembled side-by-side for a pre-remix check) is a later polish on this same tab. Reads use the authed admin (`is_admin` RLS); **NO service-role.**
- **Onboarding submission record (per client) [BUILT — C-3c-2b]** — an **IMMUTABLE, read-only** view of the RAW as-submitted onboarding answers (JSON at `client-assets` `{client_id}/onboarding-submission.json`), rendered on the **Review & Finalize** tab; read via the **authed admin** (`is_admin` storage RLS → `download`/signed URL — **no service-role**); graceful "no submission on file" empty state. **Distinct from** the editable config in Settings (prefilled from it). Data-only, no migration. (`/onboard-from-form`.)
- **SEO** (`/admin/seo`) — **[BUILT — SEO-STORE-3a, 2026-07-02]** per active client, the **SEO map capture** surface: the agency defines/confirms the client's **topical Core-30 map** — a `primary_category` + 3–4 categories, each grouping its services — stored as additive JSON at **`template_vars.seo`** (`{ primary_category, categories:[{name, slug, services:[{name, slug}]}] }`). **AI-seed** ("AI-seed from services") proposes the map from the free-text `template_vars.services` via the Lovable AI gateway (`proposeSeoMap` — **never writes**); the agency edits, then saves. **NO geographic/location pages in this map** (geo is diagnostic-driven/post-launch — `/seo-content` §7). **Save = `saveSeoMap` (server-side merge) [LOCKED]:** the panel sends ONLY the `seo` map; the fn re-reads the client's current `template_vars` server-side and overlays ONLY the `seo` key, writing the full merged object back — eliminating stale-blob clobber (two-tabs / edit-racing-a-reseed / edit-during-AI-seed). **Prefer this server-side-merge pattern over the client-side read-merge-write for large/structured `template_vars` sub-objects.** Slug validation: `^[a-z0-9-]+$`, unique across the whole map, none in the reserved-route list. Reads via authed admin (`is_admin` RLS); writes via role-gated service-role fns (`assertAgencyAdmin`). This map is the source `seedCoreThirty` (SEO-STORE-3b) reads to write the Core-30 `content_pages` rows. **[BUILT — SEO-STORE-3b, 2026-07-02]** a **"Generate Core-30 (draft)"** button on this tab calls `seedCoreThirty` → writes deterministic **draft** `content_pages` rows (1 home + 3–4 category + service pages: city+keyword titles/H1 per `/seo-build` §2, `internal_links` + real inline editorial `<a>` in body, `status='draft'`), **idempotent** (upsert `onConflict client_id,slug ignoreDuplicates` — new pages added, existing agency-edited/published rows never overwritten); a persistent inline "last run" line reports `written/skipped`. **[BUILT — SEO-STORE-3c-1, 2026-07-03]** the tab also has the **`content_pages` management surface** — the pages are visible + manageable in-panel (no SQL): a **LIST** of the active client's pages (title · type · slug · **resolved URL** [`home`→`/`, `category`/`service`→`/services/{slug}`, `geo`→`/service-area/{slug}`, `supporting`→`/{slug}`] · draft/published status pill), **edit** (title/meta/h1/body/target_keyword/internal_links/og_image/external_link/schema_jsonld; **`body` = raw-HTML monospace textarea**; **`slug`/`type`/`client_id` immutable** — the update fn's patch is `.strict()` and rejects them), **publish/unpublish** (`setPageStatus` — sets `published_at` on publish; a published page then renders via the STORE-2 live routes + an **"Open live"** link to `{allowed_origins[0] ?? template_vars.company_website_link}{path}`, hidden when the client has no domain), **preview** (in-admin **`<iframe srcdoc sandbox="">`** — required because drafts can't use the published-only STORE-2 anon RPCs; renders `h1`+body incl. the editorial `<a>` links), and **hard delete** (`window.confirm` warns that re-running Generate resurrects Core-30 slugs). **Reads via authed RLS `supabase` (admin tenant-read); ALL writes via role-gated service-role fns** (`updatePage`/`setPageStatus`/`deletePage`, `statusCode`-on-throw). **[BUILT — SEO-STORE-3c-2, 2026-07-03]** each page row has a per-row **"AI-write"** action + a client-side sequential **"AI-write all"** batch (`aiWritePage` — one `generateText` call/page, Worker-safe, per-page error collection) that rewrites the **body only** (title/h1/slug/type/internal_links/status untouched) with locally-specific, **anti-hallucination-guarded** content (only-provided-facts; see `/seo-content`), starting at `<h2>` (one-H1 preserved) with the editorial `<a>` links inline; a persistent **ai-written badge** renders from a durable `<!-- ai-written -->` marker in the body (not a content-shape heuristic). **[BUILT — 2026-07-04]** the page edit dialog also has an **"Outbound authority link"** field (saved to `content_pages.external_link`); when set, AI-write weaves ONE natural in-content link out to it (`<a rel="noopener">`) — **never invents a URL** if blank (the on-page "outbound" link type; INBOUND backlinks are off-site agency-ops, see `/seo-content` §6). All map/seed/write paths **Title-Case** names (slugs stay lowercase; acronyms preserved). **[BUILT — SEO Slice 1, 2026-07-04]** AI-write content is now **differentiated by page type** (home overview-hub / category router / service deep-dive per `/seo-content` §3), well-formatted (≥3 sections, bulleted list, `<h3>`s), and ends with a **CTA using the client's public phone** (`twilio_number ?? phone_display`; no number → no fabricated one). **[BUILT — SEO Slice 2 + 2.5, 2026-07-04]** pages carry **2-3 images** in an additive `content_pages.images` jsonb (`{url, alt, position:hero|inline-1|inline-2, width, height}`; `og_image` = hero; deterministic alt `"{business} {h1}"`; client-side natural dims). Assigned two ways, both writing an **`{images, og_image}`-only** patch via `updatePage` (never touches `body` → AI-write-safe): the per-page **ImagesManager** (3-slot picker + auto-suggest in the edit dialog) and the **Photo-Board** (a pool of the client's merged/dedup'd `site_assets` with category chips/filter → **HTML5 drag-and-drop** onto per-page slots + auto-suggest-all + per-row **"empty slots: N"**). The **preview modal** shows an **"Assigned images"** strip (hero/inline thumbnails + alt) above the body iframe. A **"Reset Core-30 / Delete all pages"** button (`deleteAllClientPages`, client-scoped, confirm-gated) clears a client's pages. **On-site image display requires the marketing-template interleave render (Slice 2 Part B).** **[BUILT — in-app setup helper]** the tab has a **"?" helper** button opening a 7-step **"How to set up SEO for this client"** checklist (check the onboarding-vs-map panel → **AI-seed from services** → review/edit to match the real GBP categories → **Save** → **Seed Core-30** → **AI-write** per page (or **Deep-write** for Pro) → **Publish**; ongoing geo + supporting ~monthly), plus an **"Onboarding vs SEO map"** comparison panel (onboarding `segment` / `service_area[0]` / `services_structured` vs the map's `primary_category` / `categories` / nested-service count, with an amber **"your N services aren't in the map yet — click AI-seed"** status). Read-only confirm aids; the setup order-of-operations they surface is authored in `/seo-build`. (`/seo-build`, `/seo-content`.)
- **Settings** (`/admin/settings`) — client profile + config (below).

## Settings tab — required configurable values (per client)
- **Go-live completion indicator [BUILT — read-only visual aid, NO data change]:** a **"needs completion before go-live"** cue set: a **light-red summary banner** listing the still-empty REQUIRED fields, a **"{n} to complete"** pill on each collapsed section header, and a **red outline** on each empty required field. Required set = `phone_display`, `tier`, `review_link`, the Telnyx-card fields (`telnyx_number`, `telnyx_messaging_profile_id`, `telnyx_texml_app_id`, `telnyx_brand_id`, `telnyx_campaign_id`, `call_forwarding_number`, `legal_address`), `notification_email`, `allowed_origins`. Purely a completeness aid for the agency before launch — it does **not** write, gate, or block anything (the actual go-live gate is `/launch-check`).
- **Timezone** — drives all SMS time windows.
- **SMS Send Window** — default **09:00–19:00** in the client's timezone. Applies to MARKETING/FOLLOW-UP SMS only (review drip, one-year drip, reactivation). Purpose: don't annoy past customers.
- **Business Hours** — a SEPARATE per-client window, distinct from the SMS Send Window. Applies to the LEAD-FORM drip only: decides whether a fresh website lead gets the live in-hours response (single SMS#1) or the after-hours single message. Purpose: "is the owner reachable / open right now." Independent value from the marketing window. [BUILD — new field, and the lead-form handler must branch on it]
- **Daily SMS send cap** — customizable per client; max messages dispatched per day. Enforced in the cron runner.
- **Daily enrollment cap** — customizable per client, **default 50**; max NEW contacts entering the review drip per day. Overflow waits to the next day. Enforced at the enrollment path (mobile-app Review Request form/button + uploads). DISTINCT from the send cap — do not conflate.
- **Business identity** — business_name, tagline, phone_display, email, address, hours, license_number, logo_url, brand_color. **Logo [BUILD — upgrade]:** today `logo_url` is a paste-only text field; add a **file-upload to `public-assets`** + an **`<img>` preview + download link** so the client's logo is retrievable from their admin view (storage layer + column already exist; the upload/preview UX does not). **Brand colors:** primary = `brand_color`; secondary/tertiary = `template_vars.brand_secondary`/`brand_tertiary` — all agency-editable (a dedicated 3-color picker is a [BACKLOG] nicety; the `template_vars` JSON editor covers it today). **One branding source, two consumers:** the same brand colors + logo set here feed BOTH the marketing site AND the per-client mobile app (see `/mobile-app` → Per-client branding [BACKLOG]).
- **Legal business address (A2P / texting-registration) [editable]** — `clients.legal_address`: the registered business address exactly as it appears on the client's IRS documents, used for 10DLC **Brand** registration (the A2P-prep pack + Telnyx/TextGrid consume it). **Captured at onboarding Step 7 (Texting Registration), surfaced EDITABLE here** so the agency confirms/corrects it against the actual IRS documents before registration (documents-first compliance). **Also surfaced in the Messaging (Telnyx) card** (same value, same merge-safe write). **DISTINCT from the public `address` above — the legal address MAY DIFFER from the public business address shown on the website.**
- **Social links (Brand & Site) [BUILT]** — friendly **Instagram / Facebook / LinkedIn** URL editors (pre-filled from `clients.social_links`), alongside the existing **"Social links (JSON)"** textarea. `social_links` is a dedicated jsonb column written **wholesale**, so the save **merges** the friendly fields into the full object (overlay only those keys; a blank field removes that key) and **re-seeds the JSON textarea** (dual-writer guard) — other keys (bbb/tiktok/yelp) preserved. Onboarding-captured socials (the wizard "Social Links" step → `social_links`) appear here pre-filled. The site footer renders `social_links` (new `linkedin` key = a template concern in `/website-structure`).
- **Review config** — review_place_id, review_link, star_threshold (default 4, inclusive ≥ — the Review Funnel gate), google_review_toggle (enum `review_gate_mode`: gated|direct). Toggle meaning: `gated` = use the funnel (rate page → branch by threshold; default); `direct` = skip the funnel, send everyone straight to the Google review page (no gate). The funnel logic in features applies in `gated` mode; `direct` bypasses `/api/public/r/rate`. (DB enum is 2-mode `{gated, direct}` — no separate "off"; to suppress review prompts, just don't enroll contacts.) The **Review link (Google URL) is AGENCY-SET here** (it must be formatted correctly to work — NOT collected at onboarding); saving it also **syncs `template_vars.review_request_link`** (the static message merge value) to the same URL via a merge-write (no clobber of other `template_vars`), keeping the column and the merge value identical. The client's **Google Business Profile link** from onboarding (`template_vars.google_business_profile_link`) is shown here **EDITABLE** (pre-filled, merge-safe save) so the agency can look the business up and correct it if it was entered wrong or changes.
- **template_vars** — free-form per-client placeholder values. Must cover all custom keys referenced by templates. Required set today: `company_owner_first_name`, `company_name`, `review_request_link`, `discount__on_referral`, `company_website_link`, `discount_amount`, `website_terms_page_link`, `quote_form_link` (defaults to `{company_website_link}` lander; overridable here — the page hosting the quote form), `about_us`, `services`, `differentiators` (the AI-knowledge content fields — F-complete Option A; anon-safe public content the chat bundle + site read). **NEVER put PII/secrets in template_vars — it's anon-readable via the `get_client_public` RPC projection (owner notification email is a dedicated column, not here).** Dynamic values (not template_vars, set at runtime): `caller_phone`, `call_time`, `request_time`, `message.body`, `feedback_message`, `email`. Surface missing keys to prompt the agency to fill them.
- **Returning Customer Discount (agency-set)** — a dedicated per-client Settings section for the 1-year follow-up campaign offer: **discount offer text → `template_vars.discount__on_referral`**, **discount amount → `template_vars.discount_amount`**. **Moved OUT of the onboarding wizard** (it's an agency campaign decision, not owner input). Edited via the page's direct-RLS save with a **read-merge-write** (writes the FULL `template_vars` with just these two keys overlaid — never a partial replace, which would wipe `about_us`/`services`/`site_assets`/etc.; re-seeds the JSON textarea as a dual-writer guard). Both keys stay in the required-keys "missing" flag when blank. **NOTE [LOCKED]:** any admin edit that touches `template_vars` MUST read-merge-write the full object — the server `updateClientSettings` fn and a raw `clients.template_vars` UPDATE both REPLACE the jsonb wholesale.
- **Messaging config [LEGACY — the "Messaging (TextGrid)" card; shown only when `clients.provider='textgrid'`]** — twilio_number, twilio_messaging_service_sid, call_forwarding_number (the client's real phone the provider number rings through to; editable per client over time). These per-client provider (TextGrid) values are NON-secret (From + Messaging Service SID; per-client subaccount under the agency master account). The master-account auth token is a single platform runtime secret; per-client subaccount creds live in the additive `provider_*` columns. (BYO-provider/BYO-Twilio per-client creds = Option 2, future, in a server-only `client_secrets` store — not here.) `sending_subdomain`/`dkim_status` are **NOT created in v1** (deferred; `ADD COLUMN` later only if per-client email is built): owner notifications send from ONE platform-level agency sender (see Email below), not per-client domains.
- **Messaging Provider select + Messaging (Telnyx) card [BUILT 2026-07-15 — dual-provider; Telnyx-only select + TextGrid card hidden 2026-07]** — a **"Messaging Provider"** select saves **`clients.provider`**; it now **offers ONLY Telnyx** (`telnyx` is the default — the TextGrid option was removed), so every new client is Telnyx. The **"Messaging (TextGrid)"** card is **hidden unless the client's provider is already `textgrid`** (legacy — kept only so existing TextGrid clients stay editable); the provider cards carry an **ACTIVE/standby badge** driven by `clients.provider` (flip = instant, reversible, both configs kept for a legacy client). The **"Messaging (Telnyx)"** card holds the **6** per-client Telnyx fields *(single-account model 2026-07-15: NO per-client credentials — one account-wide `TELNYX_API_KEY` secret; the former Managed Account ID + per-client API key fields/columns are REMOVED)*, labeled with the 5-step provisioning runbook (`/telnyx-provider` §4): `telnyx_number` (E.164-normalized on write, same server-side normalizer as `twilio_number`/`call_forwarding_number`; **never publish the number until flip**), `telnyx_messaging_profile_id` (profile webhook → `telnyx-sms-inbound`, auto opt-out DISABLED), `telnyx_texml_app_id` (voice → `telnyx-voice-inbound` / status → `telnyx-voice-status`), `telnyx_brand_id`, `telnyx_campaign_id`, `telnyx_a2p_status` (same enum as `a2p_status`) — **plus `clients.legal_address`** (the A2P legal business address now also lives in this card, same value + merge-safe write as the standalone Settings field below). Admin · Health shows **`telnyxApiKeySet` / `telnyxPublicKeySet`** badges ("Provider config" row).
- **Chat Widget card [BUILT CW3-R 2026-07-16; confirmation field added 2026-07-17]** — per-client: **"Chat widget enabled"** Switch → `clients.chat_widget_enabled` (boolean, default true; exposed in `get_client_public` so the marketing template knows to render the bubble) + **TWO message textareas**, both `template_vars` keys written via the LOCKED read-merge-write full-object pattern: **"Widget greeting message"** → `chat_widget_greeting` (shown in the bubble pre-submit) and **"Confirmation message"** → `chat_widget_confirmation` (shown as the business's in-chat reply after a visitor submits). Widget mechanics + defaults: `/chat-widget` skill.
- **Email sender (platform-level, NOT per-client)** — owner notifications send via **Resend** from ONE verified agency domain (`alerts@notif.pierceworks.co`) with a display name; no per-client sending identity in v1 (emails go to business owners, not their customers). Per-client sets the **notification recipient email** (`clients.notification_email`, below) AND an **email on/off toggle** (`clients.email_notifications_enabled`, default true) — both editable here in Admin · Settings (a Switch beside the recipient field) and self-serve by the owner in the mobile app (Account → Notifications). Implemented 2026-07-14.
  - **Single-source rule [LOCKED]:** the provider (TextGrid) number is stored ONCE (on the `clients` row) and everything that displays or uses it READS from that single source — the marketing site (header/contact/footer, via a merge value), all SMS automations, and missed-call detection. Changing `twilio_number` in admin updates everywhere on-site + in automations automatically; never hardcode the number into a page. External placements (Google Business Profile, printed business cards) are set manually outside the system and are NOT auto-updated.
- **Notification recipient email** — the email address owner notifications are sent to. Stored in the **dedicated `clients.notification_email` column** (NOT projected by the `get_client_public` RPC — owner PII, anon-denied 42501 like `clients.email`; NOT in `template_vars`, which the RPC also defensively strips it from). Defaults to `clients.email`; resolved at send time by `resolveOwnerEmail()` = `notification_email ?? email` (single-source, same posture as `twilio_number`). Settings strips any pasted `notification_email` key from `template_vars` on write. [F-pii, 3g]
- **Marketing domain(s) / allowed origins** — agency-set; the client's site domain(s); powers the public-write CORS allowlist (see opt-in-forms / scratch-foundation).
- **Finalize & Invite (launch handoff) [BUILT — C-3c-1; moved to Review console in C-3c-2b]** — a shared **`<FinalizeInvite/>`** component on the per-client **Review & Finalize** console (`/admin/review`) — **removed from Settings** (single surface, single source) — with: the **Remix handoff checklist** (read-only — `slug`, `VITE_CLIENT_SLUG` (= slug), `allowed_origins`; the site_style row was removed 2026-07-22 — the agency picks the template shell outside the app) + a **Finalize & Invite** button that mints the client login via **`provisionClientOwner`** (invite to the business notification email = `notification_email ?? email`; audited `client_owner` grant, `actor_source='fn'`). **Manual + idempotent**, `window.confirm`-guarded (sends a real email); admin/`agency_owner`-gated server-side; the page uses **NO service-role**. Carried config prereqs before a REAL invite: `VITE_INVITE_REDIRECT_URL` → the client-PWA set-password route + custom SMTP (default invite → spam). **[BACKLOG — UX]** the success toast isn't reliably visible on this page — add a persistent "Invited ✓" / inline success state for clear feedback on real-client invites. **[BUILT — C-3c-2a]** this action also flips **`status` pending→active** (direct-RLS update): wizard creates start `status='pending'` (dormant — no automations, not anon-visible), and Finalize & Invite activates them. **[BUILT 2026-07-22]** a **"Decline & delete"** secondary action sits beside Finalize & Invite for `pending` clients — **permanent full purge** (confirm-guarded `declineOnboardingClient`: storage prefixes + hard row delete via FK cascades; hard guard refuses non-pending; also on the `/agency/onboarding` queue rows — see `/agency-view`). **[BUILT — C-3c-2b]** a **missing-required-`template_vars`** reminder renders above it on the Review tab (shared `src/lib/clients/required-template-vars.ts` — single source, agrees with the Settings "missing keys" flag) so the agency fills gaps before inviting.

## Notes
- **All onboarding values are surfaced + editable here [LOCKED]:** every field captured by `/onboard-from-form` (business identity, review config, template_vars, timezone, social_links, messaging-provider (Telnyx default)/forwarding, marketing domains, notification email, legal business address (A2P), hours/business hours, service areas, discounts) must have an editor in Settings so the agency can review/adjust anything that was prefilled from onboarding. No onboarding-captured value should be invisible or uneditable in admin.
- `createClient` currently accepts only slug, business_name, phone_display, email; everything else is set here via `updateClientSettings` (or, for fields without a UI editor yet, via direct SQL until editors are added). Adding editors for those is the natural extension of this skill.
- `brand_color` is stored as hex but is NOT yet wired into the oklch theme tokens — per-client theming requires injecting a converted oklch value into the shell head (handled by the design layer, `/website-structure` §9c, which absorbed the retired `/theme-to-brand`).
- Caps and windows are the ban-protection surface: a cap or window that is only a UI field does nothing — confirm the cron runner (send cap + SMS window) and the enrollment/lead-form paths (enrollment cap + Business Hours) actually enforce these values.
