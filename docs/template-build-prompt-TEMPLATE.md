# Reusable Style-Template Build Prompt — MASTER (v2.1, 2026-07-27)

> **What this is:** the canonical Lovable build prompt for spinning up ANY new style template from design reference images. v2 was reconciled line-by-line against the SHIPPED reference template (`professional-landscpaing-template` @ `d9f512d`) and every invariant landed 2026-07-23 → 2026-07-27. **v2.1 restructures the fill experience:** all variables live in ONE `BUILD PARAMETERS` block at the top of the paste body — edit only that block, attach your images, paste the whole thing. Nothing below the line is ever edited; the body resolves every reference from the block, and looks up the style's voice/visual/slug from the imported `website-structure` skill itself.
>
> When the build reports done, paste PROMPT 2 (`docs/template-build-check-prompt.md`) into the SAME project as the acceptance gate. A template is not blessed for remixing until PROMPT 2 is all-PASS **and** Claude's external post-publish probe (PART C of that doc) passes.
>
> **Consider Mode 2 first (cheaper):** if the new design is structurally close to an existing blessed template, REMIX that template and restyle it (visual-only edits) — all wiring inherited free. Use THIS prompt when the layout/structure is genuinely new.

## Setup (operator, before pasting — ~5 minutes)

1. **New Lovable project, frontend-only** — DECLINE Cloud/DB/auth. Name it `Template — {Style}`.
2. **Import skills** (GitHub tree URLs): `template-builder`, `website-structure`, `opt-in-forms`, `a2p-site-compliance`, `seo-build`, `chat-widget`.
3. **Paste the project knowledge**: `docs/knowledge/project-template.md` into the project's Knowledge.
4. **Attach your reference images** to the prompt message (the two kinds you normally have): your **lander / web-page content examples** (= PAGE-LAYOUT refs) and your **layout/branding/font/typography images** (= BRAND-STYLE refs). No real-business logo anywhere.
5. Fill the `BUILD PARAMETERS` block below → paste the whole PROMPT 1 body.
6. After build + check pass: GitHub-connect, publish, Claude runs the external probe, then freeze/bless.

---

## PROMPT 1 — the build (edit ONLY the parameters block, attach images, paste everything)

```
══════════════ BUILD PARAMETERS — EDIT ONLY THIS BLOCK ══════════════
STYLE: <one of: Professional Modern | Artistic Unique | Corporate | Modern Tech | Family Owned / Local Business | Owner Operated / Local Business>

DEMO NICHE: <the demo business vertical, e.g. Plumbing>

DEMO BUSINESS NAME (fictional, invented — never a real business): <e.g. Bluewater Plumbing Co.>

DEMO SERVICES (3–5, realistic for the niche): <e.g. Drain cleaning, Water heater repair, Leak detection, Repiping>

ATTACHED IMAGES:
  PAGE-LAYOUT refs = <name/describe the attached lander / page-content example images — the page structure, sections and flow to reproduce>
  BRAND-STYLE refs = <name/describe the attached branding / typography / font / palette images — the visual feel and type to reproduce>

NOTES (optional, may be empty): <any extra direction — e.g. "full-bleed hero", "keep nav minimal">
══════════════════════════════════════════════════════════════════════

HOW TO RESOLVE THE PARAMETERS (do this silently; never ask me to re-confirm them):
- Every reference below to "the STYLE", "the DEMO NICHE", "the DEMO BUSINESS NAME", "the DEMO SERVICES", the PAGE-LAYOUT refs or the BRAND-STYLE refs means the value in the block above.
- Look up the STYLE's slug key, its Copy voice, and its Visual/photo direction VERBATIM from the imported website-structure skill's "Site styles" table. If the STYLE is not one of the 6 in that table, STOP and say so — a new style is added to the skill first, never invented here.
- Derive template_vars.segment as "Home services / {DEMO NICHE}" unless NOTES says otherwise.
- Consent category strings: use the DEMO NICHE's entry in the a2p-site-compliance niche library when one exists, else the DEFAULT entry — state which you used in your final report.
- NOTES may refine visuals/layout but can NEVER override a HARD RULE or a LOCKED contract below.

Build a frontend-only, data-driven client-site TEMPLATE per the imported template-builder + website-structure + opt-in-forms + a2p-site-compliance + seo-build + chat-widget skills. This is the reusable STYLE shell; the niche is a separate DATA layer — we seed the DEMO NICHE via the demo client to prove composition; do NOT hardcode anything niche- or style-specific into components. Where this prompt and a skill conflict, THIS PROMPT WINS (it is newer); where this prompt is silent, the skills are the authority; if neither covers a needed decision, FLAG it in your final report instead of guessing (finite generation — every template must resolve these identically).

MIMIC THE ATTACHED REFERENCES for visual/layout treatment ONLY — the PAGE-LAYOUT refs are the structural spec (sections, hero, page flow) and the BRAND-STYLE refs are the visual spec (palette, typography, imagery treatment); reproduce them faithfully, not as loose inspiration. The PAGE SET, routes and page identities come from the canonical page registry in /website-structure, NEVER from the reference labels. Label precedence [LOCKED]: every nav/heading label must be one of that page's allowed_display_labels; if a reference shows a word outside the set, use the closest allowed value — the reference NEVER overrides the registry. Compliance pages keep FIXED labels (Terms of Service / Privacy Policy / SMS Program).

════ HARD RULES (violating any of these fails the build) ════
1. Frontend-only. No Lovable Cloud, no database, no auth, no service-role key, no DB-writing server functions. The backend already exists; this site only READS via anon RPCs and POSTs to /api/public/* endpoints.
2. ZERO hardcoded business/niche values in components — name, phone, tagline, services, hours, colors, logo, photos, niche words. Everything renders from the client object via useClient() / the loaders. This includes route head() meta: titles/descriptions/og:* derive from the LOADED client, never literals, and no route file ever imports the demo client directly.
3. Do not invent data shapes. Use the exact contracts below. If the design needs content the contract doesn't carry, render from client.template_vars.<new_key> with a fallback and FLAG the new key in your report.
4. NEVER name a file *.client.* (e.g. foo.client.tsx) — Lovable's SSR build strips such files from the server bundle and the publish crashes.
5. NEVER import "@tanstack/react-start/server" (getRequest/getURL) from any module the client graph can reach — it fails the production build even behind typeof-window guards and dynamic import. Route SERVER HANDLERS (server: { handlers: { GET: async ({ request }) => … } }) DO receive request and may use new URL(request.url).origin.
6. No Turnstile anywhere — no widget, no keys, no turnstile imports. Bot protection is EXCLUSIVELY the invisible PoW shield specified below.
7. Compliance copy is reproduced BYTE-FOR-BYTE from the a2p-site-compliance skill's "Appendix — Canonical Verbatim Copy" — rendered as normal JSX (never dangerouslySetInnerHTML), whole documents (never excerpts), {tokens} substituted only, straight ASCII quotes exactly as the appendix has them (never smart quotes or HTML entities). Never paraphrase or regenerate legal/compliance prose.
8. The route set and shapes below are a LOCKED cross-system contract (the platform's AI writer emits internal links assuming EXACTLY these URLs and strips any others). Do not rename, merge, or restructure any route.

════ PHASE 1 — project config + data foundation (build this FIRST) ════
.env — exactly these five lines:
  VITE_SUPABASE_URL=https://onbhnkylzadyldpziapo.supabase.co
  VITE_SUPABASE_ANON_KEY=<the platform anon key — copy it from the imported project knowledge / an existing template>
  VITE_PLATFORM_API_HOST=https://app.pierceworks.co
  VITE_CLIENT_SLUG=
  VITE_SITE_URL=
Above VITE_SITE_URL add the comment: "# Set at domain-day to the client's live domain (https://theirdomain.com). Controls canonical/og:url + flips the site from noindex to indexable. NEVER put the platform/backend host here — that's VITE_PLATFORM_API_HOST and must stay https://app.pierceworks.co."
package.json: add  "overrides": { "seroval": "^1.5.2" }  so the first publish clears Lovable's dependency audit.

src/lib/client-data.tsx — the single client loader. CRITICAL: Lovable's remix process REGENERATES .env and STRIPS the VITE_SUPABASE_* lines, so the Supabase URL, the anon key, AND the API host must ALSO live as HARDCODED fallback constants in this file, pattern: const SUPABASE_URL = (import.meta.env.VITE_SUPABASE_URL ?? "").trim() || "https://onbhnkylzadyldpziapo.supabase.co"; (same for the anon key, and API_HOST fallback "https://app.pierceworks.co"). These are identical for every client forever — hardcoding is correct by design. Only VITE_CLIENT_SLUG stays purely env-driven: blank = demo mode, and it is the ONE per-remix value. Exports: isDemoMode(), getClientSlug(), getPlatformHost(), fetchClient() (POST {SUPABASE_URL}/rest/v1/rpc/get_client_public with body { _slug } and anon apikey/Authorization headers; ANY failure returns the demo client), getClientCached() (60-second TTL memo over fetchClient — shared by the root loader AND every route loader so SSR'd head() metadata always reflects the LIVE client), ClientProvider/useClient(), and ThemeStyle (brand CSS-var injection).

src/lib/content-pages.ts — the SEO content store reader. SAME hardcoded fallback constants (this file fetches independently — a remix missing them silently kills every content page while the rest of the site works; this exact bug shipped once). Anon RPCs, published rows only: fetchClientPages() → rpc get_client_pages { _slug }, fetchClientPage(pageSlug) → rpc get_client_page { _slug, _page_slug }. Normalize rows to a ContentPage shape { page_slug, type: "home"|"category"|"service"|"geo"|"supporting", title, h1, meta_description, body, body_format?: "markdown"|"html", robots?, schema_jsonld?, internal_links?, images? (PageImage[]: { url, alt, position: "hero"|"inline-1"|"inline-2", width?, height?, focal?: {x,y} }), area_served?, parent_slug?, og_image?, external_link?, updated_at? }. Include renderMarkdownLite() and splitBodySections() helpers. Do NOT export any SITE_URL constant from this or any module.

src/lib/site-url.ts — runtime origin + indexability. resolveSiteUrl(): VITE_SITE_URL (trimmed, no trailing slash) → window.location.origin in the browser → the template's own published origin as a last-resort constant. isIndexableOrigin(origin): FALSE whenever VITE_SITE_URL is unset OR the host ends with ".lovable.app"; true otherwise. (Remember hard rule 5: no server-only imports here — this module is client-reachable.)

src/lib/demo-client.ts — a FULLY FICTIONAL business of the DEMO NICHE, named the DEMO BUSINESS NAME (invented phone/email/address; zero real-business identifiers; logo_url EMPTY so the text-logo fallback is exercised). Shape it EXACTLY like the live contract — the same key names the platform actually populates, NO demo-only content keys: clients columns (slug, business_name, tagline, phone_display, email, address, license_number, hours in the canonical {"mon":["09:00","17:00"],…} shape, logo_url, brand_color, service_area[], social_links, review_link, chat_widget_enabled) + template_vars (segment per the resolution rules above, customer_care_category + marketing_category per the resolution rules, privacy_url:"/privacy", terms_url:"/terms", optin_url:"/contact", support_email, contact_email, effective_date, contact_person, brand_secondary, brand_tertiary, company_owner_first_name, company_name, company_website_link, discount_amount, discount__on_referral, review_request_link, lead_form_headline, lead_form_subhead, lead_form_cta, chat_widget_greeting, chat_widget_confirmation, about_us (2–3 factual sentences), differentiators, services_structured (the DEMO SERVICES as [{name, slug, description}] — sections LOOP over this, never fixed cards), site_assets manifest with some categories filled and AT LEAST ONE EMPTY (exercises niche-default fallbacks; staff entries carry type:"individual" {name, position} or type:"group" {label})). Demo brand colors: DERIVE dominant + accent from the BRAND-STYLE refs (never the #bd703e fallback when references exist). Also bundle niche-default images under src/assets/niche-defaults/ (a NICHE_DEFAULTS object: hero + gallery[] of generic, non-branded imagery fitting the DEMO NICHE).

src/lib/default-copy.ts — the Content-Studio copy resolver. Exactly EIGHT slots: home.hero_sub, home.services_intro, services.index_intro, about.overview, about.approach, contact.intro, gallery.intro, discount.sub. resolveCopy(slot, client) = template_vars.content?.[slot] (agency/AI override, non-blank string) → else buildDefault(slot, client), a clean merged default composed ONLY from live data fields (business_name, segment, services, service_area, hours…). LOCKED: owner-typed about_us / differentiators / tagline are AI INPUTS and are NEVER rendered raw as page copy — the resolver has no raw-display tier. The three SEO-critical slots (home.hero_sub, home.services_intro, services.index_intro) build defaults in plain search language naming the primary trade + city.

Utility modules: src/lib/hours.ts (normalize BOTH the canonical array shape and legacy {open,close,closed} objects through one helper; absent/malformed day renders "Closed", never crashes; formatters tolerate non-string input) · src/lib/phone.ts (normalizeToE164 for tel:/payloads; formatPhoneUS for ALL visible phone text — "(419) 750-0242"; tel: hrefs and JSON-LD telephone stay E.164/digits) · src/lib/hex-to-oklch.ts (brand hex → oklch theme tokens; theme from clients.brand_color + template_vars.brand_secondary/brand_tertiary injected via ThemeStyle in the shell head) · src/lib/site-assets.ts (galleryUrls(), galleryAssets() — same order, full objects with width/height metadata — and findAssetByUrl()).

src/lib/img.ts + src/components/site/SiteImage.tsx — the image delivery contract [LOCKED]: imgUrl(url, {width, height?, quality?}) swaps /storage/v1/object/public/ → /storage/v1/render/image/public/ appending width, height+resize=cover when height given, quality default 78, and passes NON-Supabase URLs (demo/niche-default imports) through untouched. srcSet(url, widths, {aspect?, naturalWidth?}) emits "url Nw, …" candidates, CAPS them at the source's natural width (when all candidates exceed it, emit exactly ONE at the cap), and returns "" for non-Supabase URLs. SiteImage wraps <img>: props { url, alt, widths, sizes, aspect?, priority?, naturalWidth?, naturalHeight?, className?, style? }; priority → loading="eager" + fetchPriority="high", else loading="lazy"; always decoding="async"; always emit width/height attributes; when srcSet returns "" omit BOTH srcSet and sizes; apply focal via style objectPosition where a focal point is provided. Use SiteImage/imgUrl for EVERY client photo on every surface. og:image ALWAYS stays the ORIGINAL object URL (social scrapers get the full file).

════ PHASE 2 — routes (the LOCKED registry — exact file names, including the [.] escapes) ════
src/routes/: __root.tsx · index.tsx (/) · about.tsx · services.index.tsx (/services) · services.$slug.tsx · locations.tsx (/locations) · service-area.$slug.tsx · $slug.tsx (catch-all, LOWEST precedence) · gallery.tsx · contact.tsx · get-your-discount.tsx · review.index.tsx (/review) · review.$token.tsx · thank-you.tsx · terms.tsx · privacy.tsx · sms-program.tsx · robots[.]txt.tsx · sitemap[.]xml.tsx

Content-page renderers (one shared ContentPageView component renders a ContentPage row: breadcrumbs, ONE h1 owned by the layout (strip duplicate leading h1/h2 from bodies), body sections via splitBodySections with inline-1 placed after the first section and inline-2 mid-body, hero image in a FIXED-HEIGHT box (h-[52vh] min-h-[380px], absolute object-cover) and inline images inside <figure class="aspect-[16/9] overflow-hidden"> with object-cover + an onError that hides the figure — these two layout invariants are what make ANY client upload safe; hero srcSet widths 640–2048 sizes 100vw eager, inline widths 480–1024 lazy; focal objectPosition; JSON-LD (LocalBusiness for home/geo, Service for category/service, BreadcrumbList) built from the resolved siteUrl; pageHeadMeta(page, path, businessName, siteUrl) producing title/description/canonical/og:* from the row + FORCING robots to "noindex,follow" whenever isIndexableOrigin(siteUrl) is false — a stored index,follow must never win on a non-indexable origin):
  - services.$slug.tsx → fetchClientPage(slug); renders ONLY type category|service; anything else = 404.
  - service-area.$slug.tsx → renders ONLY type geo; else 404.
  - $slug.tsx → renders ONLY type supporting; else 404. Every static route above beats it.
  - locations.tsx → "Service areas" index: lists the client's published GEO pages as links to /service-area/{slug} (plus the service_area strip); graceful empty state pre-SEO.
  - index.tsx (the lander) → fetches the published "home" content page: when present its H1/title/schema/body WIN (render the body's H2-per-category sections + editorial links); when absent fall back to data-driven copy via resolveCopy. TITLE FALLBACK CHAIN [LOCKED]: published home title → "{segment} in {first service_area} | {business_name}" → "{segment} | {business_name}" → "{business_name}" — NEVER "Welcome"/"Home". Homepage also carries: the hero-embedded lead form card, the services overview LOOPING template_vars.services_structured with per-card "Learn more" → /services/{slug} (these 404 until the client's SEO pages publish — expected), a Google Maps embed (iframe https://www.google.com/maps?q={encodeURIComponent(address)}&output=embed, rendered ONLY when address is non-blank, styled to the shell), hours, service-area strip, review CTA → client.review_link, and LocalBusiness JSON-LD (from the home row's schema_jsonld when published, else built from client identity).
  - review.$token.tsx → IMMEDIATE redirect (server 302 where the framework allows, else instant client-side) to https://reviewbatch.com/api/public/r/$token — no UI, no content, never in nav. This exact behavior is load-bearing for review SMS links; never remove it.
  - review.index.tsx (/review) → the "Review Us" page: a working CTA to client.review_link ONLY. NO comment box, NO /api/public/intake POST from this page (intake hardcodes source=web_form — a comment box here would mint fake leads into the lead drip).
  - get-your-discount.tsx → the discount funnel (DiscountForm below), branded via the shell; LINKED FROM NOWHERE — not nav, not footer, not sitemap; reachable only by direct/SMS link. thank-you.tsx → post-submit confirmation, also excluded from the sitemap.
  - robots[.]txt.tsx → dynamic SERVER route (no static public/robots.txt): GET handler resolves the origin from new URL(request.url).origin (fallback resolveSiteUrl()); isIndexableOrigin(origin) ? "Allow: /" : "Disallow: /", ALWAYS followed by an absolute "Sitemap: {origin}/sitemap.xml".
  - sitemap[.]xml.tsx → dynamic SERVER route: origin from the request; <loc> entries = the 6 statics (/, /services, /locations, /about, /contact, /gallery) + every PUBLISHED content page whose robots isn't noindex, mapped by type per the vocabulary (home → /, category/service → /services/{slug}, geo → /service-area/{slug}, supporting → /{slug}). Discount, thank-you, review and compliance pages stay OUT of the sitemap.
  - Every route's head() derives from getClientCached() loader data (SSR must show the LIVE business in <title>/OG — never the demo), and static routes emit canonical + og:url from the resolved siteUrl with the same forced-noindex rule.

Header/nav + Footer: nav composition fits the style/reference but links only registry pages with allowed labels (Home, About, Services, Gallery, Contact; Reviews optional). Footer on EVERY page: named Privacy Policy / Terms of Service / SMS Program links (FIXED labels), business identity, formatted phone (formatPhoneUS visible, tel: E.164), social_links (render only the present ones), license_number trust line when present. Logo component: clients.logo_url with a text-rendered business_name fallback.

About page: renders resolveCopy("about.overview") + resolveCopy("about.approach") — NEVER raw about_us — plus the about image and Team. Static-surface image slots [LOCKED]: about image = template_vars.site_slots?.about_image → galleryUrls[1] → galleryUrls[0] → work_examples[1] → NICHE_DEFAULTS; team photos = site_slots?.team (in order) → staff entries' own images → names-only (individual staff = headshot + name + position; group = single photo + label); gallery grid = site_slots?.gallery (in order) → galleryUrls → work_examples → NICHE_DEFAULTS. The LANDER HERO is deliberately NOT in site_slots — it resolves from the home content page's hero image (heroImg?.url → galleryUrls[0] → work_examples[0] → NICHE_DEFAULTS.hero). Gallery page: the grid via SiteImage (widths 320–768, sizes "(max-width: 640px) 50vw, 320px").

════ PHASE 3 — forms + bot shield + chat widget (wire EXACTLY — the backend is fail-closed) ════
Invisible PoW bot shield (src/lib/bot-shield.tsx) on ALL THREE forms: on mount POST {getPlatformHost()}/api/public/challenge with body { slug: getClientSlug() } (the slug body is REQUIRED — tenant resolution on unlisted origins depends on it; without it the fetch dies on CORS and submits carry no token) → solve the SHA-256 leading-zero-bits puzzle in a WEB WORKER (never the main thread) → produce pow_token = "challenge.sig.nonce"; refresh the challenge before its ~10-minute TTL. Also a <HoneypotField/> rendering a hidden text input named "website" positioned off-screen (absolute off-viewport — NEVER display:none). FAIL-SOFT by construction: no shield failure may ever crash a form or page; demo mode skips the challenge entirely. Zero visible UI, zero reserved height.

ONE shared LeadForm component owns all lead wiring (fields, validation, shield, POST, consent, the three copy vars) — rendered in TWO placements passing layout props only: (a) IN the homepage hero as an elevated card (desktop: right column beside the headline; mobile: stacked within the hero, visible without scrolling — NOT a separate below-hero section), (b) full-size on /contact. FIT RULE: the entire hero card (title → submit) fits the hero viewport at 1366×768 and 1440×900 via a compact variant (tighter paddings/gaps/input heights, type stepped down one size, inputs ≥44px touch height) — NEVER transform:scale(); mobile compacts further. HEADER STRUCTURE: headline + optional subhead in ONE <header> wrapper that owns the bottom margin (removing lead_form_subhead never collapses the gap); exactly one headline element. Copy: template_vars.lead_form_headline (fallback "Request a Quote"), lead_form_subhead (omit element when unset), lead_form_cta (fallback "Get a Quote"). All other quote CTAs across the site route to /contact.
  Lead form → POST {host}/api/public/intake with EXACTLY { first_name, last_name?, phone_e164, email?, notes?, website, pow_token } — phone REQUIRED (label "Mobile phone", normalized client-side to E.164 ^\+[1-9]\d{6,14}$, inline error blocks submit), email OPTIONAL, message field posts as "notes". Consent: the SINGLE-CHECKBOX marketing skeleton, UNCHECKED and NOT required (display-only — intake has no consent field; the form submits without it): "I consent to receive {marketing_category} from {business_name} at the phone number provided. Message frequency varies, up to 4 messages per month. Message & data rates may apply. Text HELP for assistance, reply STOP to opt out." with Privacy Policy (/privacy) · Terms of Service (/terms) links.
  Discount form → POST {host}/api/public/discount with EXACTLY { first_name, last_name?, phone_e164, your_message?, consent, website, pow_token } — phone REQUIRED; consent is a REQUIRED checkbox that gates submission and sends consent: true (same marketing skeleton + policy links); message field posts as "your_message".
  Chat widget (per the /chat-widget skill) — a CAPTURE-FIRST lead form in a chat skin, NO AI: floating bubble rendered only when client.chat_widget_enabled; opens greeting (template_vars.chat_widget_greeting fallback) → name/phone capture → REQUIRED consent (same skeleton) → POST {host}/api/public/chat/optin with { first_name, last_name?, phone_e164, consent: true, website, pow_token } → confirmation copy (template_vars.chat_widget_confirmation fallback). Runs the same shield.
  Universal form rules: tenant is resolved server-side from the request Origin — NEVER send client_id, NEVER send source. Demo mode: all three forms no-op with a toast ("Demo mode — submission disabled"), no POST, no error. Success routes → /thank-you (lead/discount) or inline confirmation (chat).

════ PHASE 4 — compliance surface ════
Privacy Policy page = verbatim appendix §B (WHOLE policy — IMPORTANT NOTICE header + SMS section + SMS Data Protection Statement + all numbered sections), Terms of Service = verbatim §A (SMS clauses 1–8 + TCPA/CTIA line + General Terms), SMS Program = verbatim §D — each as JSX components (e.g. src/components/compliance/*Content.tsx), {tokens} filled from template_vars ({business_name}, {support_email}, {contact_email}, {effective_date}, {contact_person}, {marketing_category}, {customer_care_category}, privacy/terms/optin URLs → the on-site routes). No literal {token} may survive to a rendered page. Straight ASCII quotes byte-for-byte.

════ PHASE 5 — design pass ════
Now style everything from the references: the STYLE's copy voice and visual/photo direction (both taken verbatim from the website-structure Site styles table per the resolution rules) steer tone and look. FONTS: match the BRAND-STYLE refs' typography as closely as possible with real web fonts (serif vs sans, weight, proportions, character) — NEVER default to Inter/Poppins and never a loose "in the spirit of" pick; if the references don't clearly show type, choose a distinctive pairing fitting the voice, then LOCK it for this style. Two-mode rule: design edits touch visuals only — the variable wiring from Phases 1–4 is untouched. Copy density and tone follow the style voice; all copy composes from live data via resolveCopy/data fields (never invented facts — no years-in-business, awards, certifications, guarantees, or "licensed/insured" claims unless the data carries them).

════ PHASE 6 — self-check, then report ════
Run the full acceptance audit in docs/template-build-check-prompt.md if it has been provided to you; otherwise run template-builder's self-check PLUS: type check passes; blank slug renders the complete demo site; grep src (excluding demo-client.ts and niche-default assets) for the DEMO BUSINESS NAME, its phone digits, and the DEMO NICHE word → ZERO hits; every route head() shows live-data titles; robots[.]txt + sitemap[.]xml respond; /review/faketoken redirects to https://reviewbatch.com/api/public/r/faketoken; all three forms carry the shield + honeypot and no-op in demo mode; no *.client.* filenames; no @tanstack/react-start/server imports outside route server handlers. FINAL REPORT: what was built, every FLAGGED decision, which consent-library entry was used, and every NEW template_vars key you introduced (each becomes an onboarding/admin field — the human must approve them).
```

---

## PROMPT 2 — acceptance gate

Paste `docs/template-build-check-prompt.md` into the same project after PROMPT 1 reports done. Remix nothing until it's all-PASS and Claude's external probe (PART C of that doc) passes.

## Freeze

After validation: GitHub-connect, publish, Claude's external probe, then the project is the remixable golden master for this STYLE — FROZEN (no per-client edits; improvements are versioned via CHANGELOG and reach existing clients only by re-remix). Per-client use: Remix (history=No) → rename `Client — {Business}` → set `VITE_CLIENT_SLUG` → publish → domain-day D.5 (runbook: allowed_origins + company_website_link + review_link_domain + `VITE_SITE_URL` + republish).
