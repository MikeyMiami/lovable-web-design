# Build Log — Stage 3f Validation (admin Automations + Upload Customers)

> Validation of Stage 3f (the two WRITE/configure admin tabs: per-client template/sequence overrides + live enrolled counts; CSV reactivation upload) against `skills/admin-view` + `automation-config` + the reuse/scoping discipline. Source: Lovable 3f report. Validated 2026-06-14 (Claude Code).
> **Verdict: PASS (logic) — clear for 3g.** Global-seed protection is airtight by construction (partial unique index + the upsert always carries `client_id`); reactivation upload genuinely reuses the 2d path; reads are `client_id`-scoped via the shared admin-context, authed-only. **The one flag that matters: 3f creates the FIRST client-override rows in the whole build (the override path was "forward-looking"/untested at 2c/2e), so the effective-template/sequence PRECEDENCE has never actually run — the recommended live walk must include a SEND/FIRE using an override, not just "a row appeared."** No tenant schema added (audit N/A). Validation mode: description-level + reasoning. No secret values.

## ✅ Validated (logic)
- **Global-seed protection — airtight on the write path.** `saveTemplate`/`saveSequence` upsert `{client_id: activeClientId, key, …}` with `onConflict:"client_id,key"` against the PARTIAL unique indexes `uq_templates_client_key`/`uq_sequences_client_key` (`WHERE client_id IS NOT NULL`). With `client_id` always a non-null UUID, conflict resolution can only match/insert a per-client row; the global rows (`client_id IS NULL`, separate `uq_*_global_key`) are excluded from that index → **structurally unreachable from this upsert.** ✓ (Completeness confirm in F-write-paths.)
- **Enrolled count.** `enrollments` `.eq("client_id", clientId).eq("status","active")` → grouped by `sequence_key` in JS, per-drip. ✓ Matches admin-view ("live count of contacts currently enrolled per drip").
- **Reactivation upload — genuine 2d reuse.** `admin.reactivation.tsx` imports `reactivationUpload` from `src/lib/enroll/reactivation.functions.ts` (the existing 2d fn) via `useServerFn`; the page only parses CSV/TSV + buckets results. The fn does E.164 normalize → contact dedupe by `(client_id, phone)` then `(client_id, email)` → `enrollReactivation` (enforces `daily_enrollment_cap` 50/day + `reactivation_20min_cap` 2/20min + the reactivation dedup guard). **No new enroll logic in the page.** ✓ Matches 2d.
- **Detail-scoping (3e bleed vector does NOT recur here).** Reads use `.or('client_id.is.null,client_id.eq.${clientId}')` (global + this client only); enrollments + reactivation stats use `.eq("client_id", activeClientId)`; active client from the shared `@/lib/admin-context`; authed browser client only, no service-role. Templates/sequences are keyed by `(client_id, key)` and the `.or` is client-aware, so a stale key under client B returns B's override/global (not A's) — and they're copy, not PII. ✓ (Minor confirm in F-by-id.)

## 🟥 The key flag — F-resolver: effective override PRECEDENCE is first-exercised by 3f
At 2c the override path was recorded as **"forward-looking; currently all loads resolve to globals (2a seeded only globals)."** 3f is the **first** code anywhere to create `(client_id, key)` override rows. So the precedence — *does an override actually get USED at send/fire time, preferred over the global?* — has **never run.** The editor writing a correct override row is necessary but NOT sufficient; the value of 3f is real only if every READER prefers the override. Confirm there is ONE consistent resolver (or consistent precedence) across **all** readers:
- **Notification dispatcher (2c `writeNotification`)** — prefer `(client_id,key)` → fall back to `(key) WHERE client_id IS NULL`.
- **SMS copy render (runner/sender, 2e)** — same precedence for message templates.
- **Sequence load (runner claim/step, 2a/2e)** — if a `sequences` override exists, the runner must run the CLIENT's sequence, not the global.
If any reader ignores the override (or two readers disagree), an admin's edit silently no-ops or applies inconsistently. **Recommended live walk (the real proof):** create a template override for client A → (a) a client-A row appears + global untouched + client B unaffected (the structural check), AND (b) **fire/send that drip for A → the message uses A's override; fire for B → uses the global.** Repeat for a `sequences` override if the editor can change steps. This is the end-to-end proof that the forward-looking override path now works.

## 🟡 Minor confirms (non-blocking)
- **F-write-paths:** confirm the ONLY mutation paths in the Automations editor are the `client_id`-carrying upserts — **no `.update()`/`.delete()` by `key` alone**, which (under admin RLS, if templates/sequences UPDATE policies permit global rows) could touch a `client_id IS NULL` global row. The upsert is safe; verify nothing else writes here.
- **F-by-id:** confirm a single-record editor loads the row by `(client_id, key)` / the `.or` scope, NOT by global `id` PK (a by-id load + stale selection would reintroduce the 3e-style vector — lower stakes here since templates are copy, not PII, but keep the discipline).
- **F-bucket:** confirm the upload result buckets (Enrolled / Duplicates / Capped / Other) cover EVERY reason `reactivationUpload`/`enrollReactivation` can return (e.g. invalid-phone, already-reactivated/`review_completed` dedup, 50/day vs 2/20min cap) — "Other" should be a true catch-all, not a silent drop.

## Status
- **3f PASS (logic) — clear for 3g.** Global-seed protection airtight (partial index + client_id-always upsert); enrolled count correct; reactivation = genuine 2d reuse; reads client_id-scoped, authed-only, shared admin-context.
- **Key flag F-resolver:** the override PRECEDENCE is first-exercised by 3f (forward-looking until now) → run the override **send/fire** live walk + confirm one consistent resolver across dispatcher + SMS render + sequence load. Plus minor confirms F-write-paths / F-by-id / F-bucket.
- **Clear for 3g (Settings)** — the per-client config surface (writes `clients` + `send_settings`); carry the same client_id-scoped-write + shared-admin-context + no-service-role discipline, and the single-source `twilio_number` + `allowed_origins`→CORS + `google_review_toggle` (2-mode `gated`/`direct`) checks.
