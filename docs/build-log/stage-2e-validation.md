# Build Log — Stage 2e Validation (drip wiring end-to-end, on stub)

> Validation of Stage 2e (all drips wired on stub send + data-driven `window_gated`) against `features` + `automation-config`. Source: Lovable 2e report (5 test walks). Validated 2026-06-09 (Claude Code).
> **Verdict: VALIDATED — NOT closed.** Drip-walking, exits, window-gating, carry-forwards all sound. 4 confirms before close + 1 forward (1f) flag. **Clear to start 2f** (independent). No secret values.
> **UPDATE 2026-06-13 (Claude Code): PASS — CLOSED.** All 4 before-close confirms returned from Lovable and validated against spec (§7 day-10 suppression, §9 reactivation-click, single-write-path). Core requirement of each is met; 3 non-blocking residuals recorded below. #5 remains a 1f forward flag. **Stage 2 feature-complete except 2f (AI chat widget).** Validation mode: spec-conformance of Lovable's described behavior (the Lovable source is not in this planning repo — same description-level basis as 2c, not a code byte-read).

## ✅ Validated
- **Step compositions match the 2a seed**: review_request 5 (4 SMS + final internal), one_year_followup 7 (sms1→sms2→after_sms2_internal→sms3→sms4→sms5→final_internal), missed_call 3 (sms1→internal→sms2), reactivation 4, lead_form 3×2, discount 2. ✓
- **`sequences.window_gated` (new boolean column)** — gated=true: review_request, one_year_followup, reactivation (marketing, §3 9–7 + daily_send_cap); gated=false (24/7): lead_form ×2, missed_call, discount. Runner: internal-notif steps never gated/capped; sms steps defer only if gated & outside-window / gated & cap-hit; non-gated → straight send. Lead-form business-hours branch lives enrollment-side (2d). TEST5 proves the distinction (review deferred, missed-call sent, same off-window tick). ✓
- **Exit semantics** match the skill: review→click (pre-step `tracked_links.clicked_at` + funnel real-time exit = belt-and-suspenders); one_year→reply (`exited_on_reply` + interest notif) / opt-out (silent); NOT on click; missed-call sms2 `skipIfReplied`; reactivation→click; discount→exits one_year on enroll (2d); lead-form→none. ✓
- **action-jsonb:** day-10 → `{auto_enroll:true}`, missed-call internal → `{open_conversation:true}`; step-supplied overrides default. ✓
- **Carry-forwards landed:** `/api/public/r/feedback` raw insert removed → `writeNotification({type:'negative_review_feedback'})` (single 2c path); AI-chat notification deferred to 2f. ✓
- 5 test walks: TEST1 review click→exited (0 SMS); TEST2 review full→4 sms_sent + final notif→completed; TEST3 one-year + simulated inbound→exited_on_reply + interest notif; TEST4 missed-call sms2 + inbound→skipped→completed; TEST5 window-gating.

## ✅ Confirm before close — ALL RESOLVED (2026-06-13, Lovable 4-confirm report)
1. **`audit_tenant_rls()` re-run after `window_gated` migration — PASS.** Lovable returned `(0 rows)` (empty table pasted: `table_name | policy_name | cmd | reason → 0 rows`). Expected: `window_gated` is a 2nd additive boolean on `sequences` (no policy change; `sequences` is a non-tenant catalog), so 0 = PASS per the SYSTEM-NOTES discipline (run after every migration). Hard evidence (the actual query output), not a claim. ✓
2. **Day-10 reminder SUPPRESSION (§7) — PASS, with 2 residual notes.** Wired in `runner.server.ts` internal-notification branch: when `step.templateKey === "lead_form_day10_owner_reminder"`, it counts `enrollments` for same `(client_id, contact_id)` with `sequence_key='review_request'` and `created_at >= thisEnrollment.created_at`; if any → `advanceStep(..., "skipped_day10_review_enrolled")` and **no notification is written**. Satisfies §7's core ("suppress the day-10 reminder if the lead is now in review automation"). ✓
   - **Residual 2a (low risk — confirm):** matches on `(client_id, contact_id)`; §7 says "the lead's *phone*." Equivalent ONLY if the Review-Request enroll path matches the EXISTING lead-form contact by `phone_e164` (one `contact_id` per `(client_id, phone)`) rather than minting a new contact row. The whole contact model is phone-keyed (§4 re-enroll guard, §9 missed-call "match by phone_e164"; 2d guards on `client_id+contact_id`), so this is near-certainly correct — but it's the one assumption the suppression rests on. Confirm contacts are phone-deduped at the review-enroll path.
   - **Residual 2b (deliberate narrowing — note):** the `created_at >= thisEnrollment.created_at` filter narrows §7's literal "is *now* enrolled" to "enrolled in review *since* this lead-form enrollment." Correct for the dominant flow (web lead arrives → owner later adds them to review → suppress). Edge case: a contact ALREADY in the review drip *before* re-submitting the web form would have `review.created_at < lead.created_at` → NOT suppressed → owner still gets the day-10 reminder. Rare for fresh web leads; acceptable, but it is a semantic choice, not a literal spec match.
3. **Reactivation-click NOTIFICATION (§9) — PASS, clean.** Wired in `/api/public/r/$token.ts`: after the idempotent first-click stamp, gated on `isFirstClick && link.sequence_key === "reactivation"` → `writeNotification({ type: "reactivation_click", ... })`. Review (or any non-reactivation) clicks do NOT fire it (branch only matches `sequence_key === "reactivation"`); `isFirstClick` prevents repeat-open spam (and the drip exits on click, so later opens are post-exit). `type:"reactivation_click"` → key `reactivation_click_internal` per the 2c mapping; fired via the single `writeNotification` dispatcher. Matches §9 exactly. ✓
4. **`writeNotificationByTemplateKey` = thin wrapper ending in `writeNotification` — PASS (single insert path), 1 residual.** Confirmed in `src/lib/notifications/write.server.ts`: maps `templateKey → NotificationType`, resolves `action ?? ACTION_BY_TEMPLATE_KEY[templateKey]`, then `await writeNotification({...})` and returns `{id, type}`. **Exactly one `.from("notifications").insert(...)`** in the file (inside `writeNotification`); the by-template helper has none → single insert path, no parallel write. Reconciles the 2c→2e carry-forward (no double-write). ✓
   - **Residual 4a (confirm — completeness):** the headline (single insert path) is proven by the excerpt; the secondary ask ("the reverse-map covers EVERY internal-notif step's templateKey") is NOT enumerated. The `templateKey→type` map and `ACTION_BY_TEMPLATE_KEY` should cover all internal-notif step keys (the 2c set of 11, incl. the 2c-added `negative_review_feedback_internal` and `lead_form_day10_owner_reminder`); an unmapped key yields `type === undefined`. Confirm the map is exhaustive over the runner's internal-notif steps.

## 🟧 Forward (1f) — design-confirm now
5. **One-year reply-exit must be REAL-TIME at 1f, not pre-step-only.** One-year inter-step gaps are weeks/months; a pre-step-only reply check delays the "they replied" interest notification up to ~30–60 days (useless for a hot lead). 2e stub uses pre-step (fine for testing). **Confirm the 1f inbound-SMS webhook drives one-year exit + interest notif in real-time** (like the funnel does for clicks). (Missed-call reply-skip is fine pre-step — 2-min gap.)

## 🟢 Minor / confirmed
- **discount `window_gated=false` ✓** (transactional form-ack, matches lead-form pattern; §2/§3 gate only review/one-year/reactivation). **Recommend documenting discount in the spec's transactional list** (currently inferred).
- **one-year opt-out (`opted_out_at >= created_at`) ✓** given 2d enroll guard + §4 handoff opt-out exclusion (≈ `IS NOT NULL` in practice). Confirm the review→one-year handoff uses the guarded enroll path.
- Missed-call enrollment + 7-day re-eligibility is a 1f item (voice webhook creates the enrollment); 2e tests the drip steps only.

## Status
- **2e CLOSED — PASS (2026-06-13).** All 4 before-close confirms validated against spec: audit re-run = 0 ✓; day-10 suppression wired (skip + no-write) ✓; reactivation-click gated on `sequence_key==="reactivation"` ✓; `writeNotificationByTemplateKey` = single insert path delegating to `writeNotification` ✓. 3 non-blocking residuals recorded (2a contact_id≡phone assumption; 2b `created_at>=` narrows "now enrolled"; 4a by-template map completeness) — confirm opportunistically in 2f / Stage 3, none block close.
- **#5 (one-year reply-exit must be REAL-TIME) remains a 1f forward flag** — production inbound-SMS webhook, not a 2e item (2e stub uses pre-step checks).
- **Stage 2 is feature-complete except 2f** (AI chat widget — last Stage 2 sub-step; reuses the lead-form pipeline; adds the deferred AI-chat-lead notification "New Website AI Chat Lead" per §7e).
- **Clear to start 2f.**
