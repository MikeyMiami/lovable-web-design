# Build Log — Stage 2f Validation (AI Chat Widget — §7e, final Stage 2 sub-step)

> Validation of Stage 2f (AI chat widget: opt-in gate → contact, FAQ stream, pricing guardrail, Request path reusing the §7 lead-form enroll, AI-chat-lead notification via source-based templateKey swap) against `skills/chat-widget` + `skills/opt-in-forms` + `skills/features` (§7e) + the 2c/2e notification dispatcher. Source: Lovable 2f report. Validated 2026-06-13 (Claude Code).
> **Verdict: PASS — CLOSED 2026-06-13.** All scrutiny points validate against the skills; closes the 2e-4a map-completeness residual. **Closing gate (unauthenticated public reachability) PROVEN on the published URL** (2b standard) — see below. 2f is closed → **STAGE 2 IS COMPLETE (2a–2f all PASS).** Validation mode: spec/skill-conformance of Lovable's described behavior (Lovable source not in this planning repo — description-level basis, as 2c/2e). No secret values.

## ✅ Validated against the skills

- **Opt-in gate (chat-widget §"Opt-in gate") — MATCH.** `POST /api/public/chat/optin`: Zod-validates First/Last/Email/Phone + `consent:true`; tenant resolved server-side via Origin/slug (CORS-resolver pattern, never request body — foundation guardrail 4); creates contact `source='chat_widget'` **server-set** (distinct from `web_form`); returns an HMAC chat_token. **Chat UI not openable without the token → no anon chat.** ✓ Matches "BEFORE the AI converses on EITHER path, the person opts in" + `chat_widget` source written via server fn (not anon).
- **FAQ path + knowledge injection — MATCH (mechanism).** `POST /api/public/chat/stream` requires `X-Chat-Token`; injects `buildSystemPrompt(loadKnowledgeBundle(claims.cid))`; `streamText(model=google/gemini-3-flash-preview)` → `toUIMessageStreamResponse`. Per-request system-prompt injection (retrieval, not fine-tuning) ✓. Gateway = Lovable AI Gateway + `LOVABLE_API_KEY` server-side; 429→`rate_limited`, 402→`credits_exhausted` ✓ — exactly the chat-widget §"AI invocation" + §9d contract.
- **Pricing/quote guardrail — MATCH (as a soft guardrail).** `buildSystemPrompt()` carries hard-rule #1 (no price/quote → redirect copy "Let me get you an accurate quote…"), plus "only this business" + "no promises". ✓ Matches the four hard rules. *(Inherent caveat: a system-prompt guardrail is probabilistic, not a hard gate — the skill itself calls it "firm instructions," not a guarantee. No invented-number path in code, which is the part that can be enforced. Acceptable; see Flag F5.)*
- **Request path reuses the §7 lead-form enroll EXACTLY — MATCH.** `POST /api/public/chat/request` verifies the chat_token, persists the message on the contact, branches on **Business Hours**, and calls `enroll({sequenceKey: lead_form_business_hours | lead_form_after_hours})` — the identical website-lead-form path, not a parallel pipeline. ✓ Matches "reuse, don't duplicate; only the notification label differs."
- **AI-chat-lead notification = source-based templateKey SWAP through the single 2c dispatcher — MATCH, no double-notify.** In `runner.server.ts`: when an internal-notification step's templateKey is `lead_form_internal_notify` **OR** `lead_form_after_hours_owner_internal` **AND** `contacts.source='chat_widget'` → swap to `ai_chat_lead_internal_notify` **before** `writeNotificationByTemplateKey → writeNotification` (the single 2c insert path). It is a **swap of the existing step, not an added write** → a chat lead gets ONLY the `ai_chat_lead` notification, never also the plain lead-form one (no double-notify) ✓. Swap covers **both** branches (BH + AH) ✓. Correctly does NOT swap `lead_form_day10_owner_reminder` — the day-10 "add them to your review form" reminder is source-agnostic, so it stays as-is (right scoping). ✓ Single-write-path invariant from 2e-4 preserved.
- **12-type NotificationType↔templateKey map — EXHAUSTIVE, closes 2e-4a.** Enumerated 12 types; cross-checked 1:1 against the spec/automation-config internal-notification inventory across every drip: review final (1), lead-form BH/AH/day-10 (2,3,4), missed-call (5), reactivation-click (6), one-year reply/after-sms2/final (7,8,9), discount (10), negative-review-feedback (11), ai-chat-lead (12). The first 11 match the 2c-recorded set byte-for-byte; #12 is the only addition. `ai_chat_lead` is not a separate STEP — it's a runtime swap of #2/#3 — so every templateKey the runner can emit resolves to a defined type; **no undefined paths.** ✓ This is the completeness evidence 2e-4a asked for.
- **Template lineage — CONSISTENT.** `ai_chat_lead_internal_notify` seeded as a global row (`client_id NULL`) like the others; migration `20260613…`. Count **30 → 31** — exactly the next increment from the 2c-recorded 30 (which itself was 29→30 for `negative_review_feedback_internal`). Lineage checks out. ✓ *(Byte-level confirm recommended — see F4.)*

## ✅ CLOSING GATE — public reachability PROVEN (2b standard, 2026-06-13)
The `/api/public/chat/*` routes are under the **correct auth-exempt public path** (Lovable applied the 2b lesson — did NOT repeat the top-level `/r/*` mistake). Reachability proven on the **published URL**, fully unauthenticated (no session, no auth header):
- `POST /api/public/chat/optin` (empty body) → **HTTP 400**, the handler's **own Zod response** listing missing fields (`first_name, last_name, email, phone_e164, consent`) → handler ran. CORS headers present (`access-control-allow-methods: GET, POST, OPTIONS`). **❌ NOT 404 · ❌ NOT 302 auth-bridge.** ✓
- `OPTIONS /api/public/chat/optin` (CORS preflight) → **HTTP 204** with full CORS headers (`allow-methods`, `allow-headers`, `max-age`, `vary: Origin`). **❌ NOT 302.** **ACAO correctly OMITTED for non-allowlisted origins** — this also confirms the §6 / guardrail-4 per-client CORS allowlist is *enforced*, not wide-open (allowed origins get echoed; unknown origins are browser-blocked). ✓
- All three routes (optin/request/stream) reachable by logged-out visitors, gated only by handler logic (Zod + chat-token) — **exactly like `/api/public/intake`.** ✓
This is the same proof shape that closed 2b. Closing gate satisfied → 2f closes.

## 🟡 Flags (none block 2f logic; F2 is a launch gate)

- **F1 — Chat token HMAC key = `CRON_SECRET` (minor hardening, non-blocking).** Works (HMAC, 1h TTL, constant-time verify; secret is server-only, not exposed). But it conflates cron-auth and chat-signing on one secret: rotating `CRON_SECRET` silently invalidates all live chat tokens (blast radius ≤ 1h TTL). Recommend a dedicated `CHAT_TOKEN_SECRET`. Separation-of-concerns, not a hole.
- **F2 — Chat opt-in + request are NEW public lead-intake surfaces → must be under the Stage-1f Turnstile/rate-limit gate (LAUNCH GATE).** `/api/public/chat/optin` creates a contact (a public lead write, like the website lead form). The report shows server-side tenant/CORS resolution but does **not** mention Turnstile/bot-protection or rate-limit on it. Per §6 + the [GATE] 1f note ("Turnstile + rate-limit on public lead-intake MUST ship before ANY client launch — CORS is browser-only"), these two endpoints must be included in 1f coverage. Mirror into `/launch-check` §E. Non-blocking for 2f logic; blocking for go-live.
- **F3 — Owner-EMAIL label variant deferred to the email layer (1f).** The in-app notification swap to "New Website AI Chat Lead" is done. §7d's owner EMAIL (currently `owner_email_stub` events in stub mode) needs the matching subject variant ("New Website AI Chat Lead", BH + AH) wired when the email layer ships at 1f — mirror the in-app swap so the email isn't left labelled "New Website Lead." Forward flag.
- **F4 — Byte-confirm the new template (recommended, as 2c).** Run `SELECT key FROM public.templates WHERE client_id IS NULL ORDER BY key;` → expect **31** keys incl. `ai_chat_lead_internal_notify`; and confirm the seeded body matches §7e copy ("…a new lead from your website AI chat!" / "We've already replied to them in the chat.") — body not pasted in the report (internal owner-facing, editable; low risk).
- **F5 — Knowledge bundle richness (minor/forward).** Bundle = `business_name` + `template_vars` (services, hours). The chat-widget skill + §7e/§9b want the full set (About Us, service areas, differentiators) too. The per-request injection MECHANISM is proven; bundle completeness is a per-client `/onboard-from-form` (Stage 5) concern — confirm `loadKnowledgeBundle()` pulls all onboarding knowledge fields when populated, not only `template_vars`. Non-blocking.
- **F6 — Route-path reconciliation applied (anti-regression, 2b-style).** chat-widget SKILL.md line 50 + spec §7e line 488 named `src/routes/api/chat.ts` — a **top-level (auth-gated) path**, the exact trap 2b caught for `/r/*`. Lovable correctly built under `/api/public/chat/*` instead. Reconciled both docs to the public path + added an anti-regression note (do NOT use top-level `api/chat.ts`). See edits below.

## Status
- **2f CLOSED — PASS (2026-06-13).** All skill-conformance points validate; 2e-4a map-completeness residual CLOSED (12-type map exhaustive); unauthenticated reachability PROVEN on the published URL (400 Zod handler + 204 CORS preflight, not auth-bridge).
- **STAGE 2 IS COMPLETE — 2a–2f all PASS.** (2a steps_json seed · 2b tracked-link + funnel · 2c notifications dispatcher · 2d enrollment paths/caps/guards · 2e drip wiring + window-gating · 2f AI chat widget.) Feature + automation logic layer done on the shared golden-master backend.
- **Forward carry (flags routed to owning stage):**
  - **F1 — `CHAT_TOKEN_SECRET`** (decouple chat-token signing from `CRON_SECRET`): hardening, do with the 1f security pass. Non-blocking.
  - **F2 — chat `/optin` + `/request` under the 1f Turnstile/rate-limit gate:** WIRED into `/launch-check` §A (line 18) + §E (line 58, GATE) — they're now named public lead-intake routes. **Launch-blocking; no client launches until covered.**
  - **F3 — AI-chat owner-EMAIL subject "New Website AI Chat Lead"** (BH+AH): wire at the email layer (currently `owner_email_stub`); mirror the in-app swap.
  - **F4 — byte-confirm** `templates WHERE client_id IS NULL` = 31 keys incl. `ai_chat_lead_internal_notify` + body matches §7e copy. **F5 — `loadKnowledgeBundle()`** pulls full §9b knowledge set (About Us / areas / differentiators), not just `template_vars` — per-client `/onboard-from-form` (Stage 5).
  - **F6 — route-path reconciliation** (`api/chat.ts` → `/api/public/chat/*`): DONE in chat-widget SKILL.md + spec §7e (anti-regression note added).
- **Next: Stage 3 — client-facing surfaces (`/admin-view` + `/mobile-app`).** These consume the notifications subsystem these drips write — incl. the `{auto_enroll:true}` (day-10) and `{open_conversation:true}` (missed-call) action flags the mobile Notifications tab reads, and the Conversations thread the chat widget feeds.
