# Build Log — Stage 3.5 item 1 (audit_log) Validation

> Validation of the role-mutation `audit_log` (Option 3: `user_roles` trigger as sole-writer/completeness-floor + fn-context enrichment via `SET LOCAL` GUC) against the spec §12 `[BUILD — TODO] audit_log` bullet + its DESIGN clause. Source: Lovable build report (with proof rows). Validated 2026-06-15 (Claude Code).
> **Verdict: audit_log FULLY CLOSED — PASS (2026-06-15).** All 7 mechanism points proven AND the security must-confirm resolved — which **found + fixed a real hole**: the audited RPCs were `SECURITY DEFINER` with ZERO authorization (trusted the server fn) = the predicted "audited but unauthorized" escalation path. Now both `assign/revoke_user_role_audited` re-run the full 1d matrix from `_actor` BEFORE the mutation; tested (client_owner blocked from admin/agency_owner/client_owner + cross-tenant; positive control allowed + audited; evil target = 0 `user_roles` + 0 `audit_log` rows since `RAISE` aborts pre-INSERT). Authz now in two places (server fn + RPC body — the latter critical, since the RPC is directly callable via `supabaseAdmin.rpc`); audit in the trigger; immutability on the table; no bypass path (zero direct `user_roles` mutations in `src/`, fns call only the audited RPCs). Audit-write failure → `RAISE WARNING` (visible). **Gate FULLY satisfied:** real admin/agency_owner grants are now both AUTHORIZED and AUDITED. Validation mode: description-level + proof rows. No secret values.

## ✅ Validated (the 7 points)
1. **Double-log / fn-path** — Proof 1 (`assign_user_role_audited`): actor `4efaaa92…` populated, `actor_source='fn'`, reason captured (the real operator via the GUC, NOT the service-role); exactly ONE row. ✓
2. **Catch-all / direct-SQL** — Proof 2 (raw `INSERT INTO user_roles`): actor NULL, `actor_source='direct_sql'`, ONE row → the trigger caught what a fn-only audit would miss. ✓ (This is the whole point of Option 3.)
3. **Never-rolls-back** — audit insert wrapped `EXCEPTION WHEN OTHERS THEN NULL`, AFTER trigger returns NULL → the role mutation commits even if the audit write fails. ✓ *(see Minor: prefer RAISE WARNING over silent NULL.)*
4. **`SET LOCAL` hygiene** — `set_config(…, is_local := true)` → txn-scoped, no pooled-connection cross-attribution. ✓
5. **Append-only + RLS** — immutability trigger `RAISE EXCEPTION` on UPDATE/DELETE for ALL roles incl `service_role`; RLS read = `is_admin()` (admin + agency_owner; `client_owner` denied); no write policies (trigger is the sole writer). ✓
6. **Backfill** — row present for the pre-audit test-admin (`4efaaa92…`): actor NULL, `source=direct_sql`, `backfilled=true` → no real admin is unaudited. ✓
7. **`audit_tenant_rls()`=0** post-migration ✓ (audit_log has no `client_id` → excluded from the tenant scan; migration didn't perturb anything).

## Q — bypass + authorization (the user's question)
**(a) No unaudited bypass exists — by design.** Even if the OLD `assignUserRole`/`revokeUserRole` somehow persisted and were called, the **trigger is on `user_roles` and fires for ANY mutation** — so the grant would STILL be audited (just with actor NULL / no reason). The trigger-as-sole-writer/completeness-floor means there is **no path to an unaudited grant**. *(Confirm, for context-completeness not completeness: the old DB fns are DROPPED / have zero callers, and `roles.functions.ts` calls only the audited RPCs — so all grants get the rich fn-context, not just a NULL-actor row.)*
**(b) ✅ RESOLVED — a REAL hole was found + fixed.** The audited RPCs were `SECURITY DEFINER` with **ZERO authorization** (they trusted the server fn) — exactly the "audited but unauthorized" escalation path: anyone reaching the RPC (it's directly callable via `supabaseAdmin.rpc`, RLS-bypassing) could grant any role and it'd just be neatly audited. **FIX:** both `assign/revoke_user_role_audited` now re-run the full 1d matrix from `_actor` BEFORE the mutation — shape checks (platform roles ⇒ no client_id; tenant roles ⇒ client_id), actor required, platform-admin grants any, `client_owner` only `client_staff`/`client` in OWN tenant, `client_owner` CANNOT grant admin/agency_owner/client_owner. **Tested:** client_owner→admin = Forbidden; →agency_owner blocked; →client_owner own = "Forbidden role for client_owner"; →client_staff OTHER tenant = Forbidden; **positive control** client_owner→client_staff OWN tenant = allowed + audited (`actor_source=fn`, reason). **Evil target uuid: 0 `user_roles` rows + 0 `audit_log` rows** — the `RAISE EXCEPTION` aborts before the INSERT, so the trigger never fires (authz gates before mutation; audit only records REAL mutations). Authz is now defense-in-depth (server fn + RPC body). *(Note: rejected attempts leave no `audit_log` row — correct, they're not mutations; they surface as errors in PG/app logs. Active alerting on repeated escalation attempts = a monitoring/IDS concern, backlog, not an audit_log gap.)*

## ✅ Minor — DONE
- `user_roles_audit()` now `RAISE WARNING 'audit_log write failed: %', SQLERRM` instead of silent `THEN NULL` → a dropped audit write surfaces in server logs (visible; the role mutation still commits — availability preserved). ✅

## Status
- **audit_log FULLY CLOSED — PASS.** All 7 mechanism points + the 1d-matrix authz (a REAL hole found + fixed) + immutability + no-bypass + visible-drop. Option-3 as spec'd, hardened.
- **GATE FULLY SATISFIED:** real admin/agency_owner grants are now both **AUTHORIZED** (1d matrix re-run in the RPC body, defense-in-depth with the server fn) and **AUDITED** (trigger sole-writer). Clear to grant real platform roles.
- **All confirms closed:** 1d-matrix retention ✅ (fixed — was a `SECURITY DEFINER`-with-zero-authz hole); old-fn drop / no-callers ✅ (zero direct `user_roles` mutations in `src/`, fns call only the audited RPCs); WARNING-not-silent-NULL ✅.
- **Remaining Stage 3.5:** item 2 **export-client** (guardrail 3) · item 3 **regression re-test** (runner-renders-real-bodies full drip set + send-primitive + `runner_version`). Then `/launch-check` A–D green (except the §A Turnstile/rate-limit launch-gate row) → Stage 4 freeze.
