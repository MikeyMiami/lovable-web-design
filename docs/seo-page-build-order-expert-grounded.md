# Expert-grounded page-build order + per-type research — FULL-CORPUS synthesis

> Verified against ALL SIX transcripts in `C:\Users\Pierc\Documents\SEO\` (full reads). Cited `<ID>:line`. **CORRECTION NOTICE:** an earlier draft of this doc (based on only ZKZnDORR0ds + a partial FifSqbB0) claimed "local-landmark research is geo-only / service pages stay service-focused / research feeds supporting-not-Core-30." **The full read of `FifSqbB0` (the content tool our 8-pass is modeled on) CONTRADICTS that.** Corrected below. No build; commits held.

## The corpus is NOT one methodology — it's several models
- **`U7bmZnUVXfc`** (3-pillars intro) + **`W3deWbeigsg`** (pricing/economics): high-level; cover none of the build/research specifics. No conflict.
- **`How to Use Content…`**: an older **40-page** model — home→category→service, **neighborhoods on the CATEGORY page** (`:133-134`), "build in an afternoon," **no geo phase, no rank gating**.
- **`pNPEz`**: a **hyper-local** model — Core-30 = service×category×**neighborhood** combos; **landmarks ON the service pages** (H1 = "service + neighborhood", `:301-318`); **Reddit/forum research feeds Core-30 selection** (`:348-358`); no rank gating.
- **`ZKZnDORR0ds`** + **`FifSqbB0`**: the coherent **"a-to-z system + content tool"** pair (they cross-reference the same Lead-Snap rank map, "core 30," topical-vs-geographic relevance). **This is the pair our tool is built on** — ZKZnDORR0ds = the sequencing/rank-map framework (our S4-B); FifSqbB0 = the 8-pass writer + research prescription (our S4-D/S4-A).

## Q1 — Build order (ZKZnDORR0ds + FifSqbB0, our model) [HOLDS]
1. **Entity/GBP research + homepage foundation** (title/H1 = primary category + city; category audit vs competitors) (`ZKZnDORR0ds:131-139`, `FifSqbB0:52-85`).
2. **Core-30 topical structure**: home → 3-4 category pages → 25-30 service pages, mirroring the GBP; home→category→service internal links (`ZKZnDORR0ds:173-194`). Service keyword = service+city, deep + specific (`ZKZnDORR0ds:196-200`).
3. **One authority external link per page** (`ZKZnDORR0ds:228-232`).
4. **Measure** — 30 days later, rank map → **top-3%** vs **threshold (25-50% of competitors' top-3%)** (`ZKZnDORR0ds:263-290`).
5. **Below threshold → supporting content** (research-driven, FAQ + deep pages) (`ZKZnDORR0ds:300-352`).
6. **At/above → geo/location pages** (landmark-centric, 10-20, **published in waves**, rank-map every couple weeks) (`ZKZnDORR0ds:352-395`).
7. **Dominating → maintain** (`:401-406`).
- **Our tool matches this** (Core-30 seed → publish → geo LOC-2/3 wave-publish, gated by S4-B `topical_status`; supporting via S4-C). ✅

## Q2 — Research per page type [CORRECTED — research is NOT supporting/geo-only]
`FifSqbB0` (the actual tool) is explicit that research is BOTH a supporting-planner step AND a universal pre-write step for every page type:
- **Universal pre-write research (ALL page types):** "before the agent writes a single word, it does multiple rounds of research. First… what real people are actually asking about each service. Reddit, local forums, **competitor sites**, people also ask results. Second, it researches **the local area itself. Neighborhoods, landmarks, local conditions**" (`FifSqbB0:343-352`).
- **The 8-pass pipeline runs for FOUR types** — "service pages, category pages, location pages, and supporting content" — differing by **writing prompt**, not by whether they get research (`FifSqbB0:392-399`).
- **Pass-1 research synthesis shown ON A SERVICE PAGE** ingests "Reddit threads, people also ask results, **competitor angles, local landmarks**… what local details to weave in, what angles are the competitors covering that this page needs to cover but better" (`FifSqbB0:409-421`).
- **Service content is explicitly meant to be genuinely local:** "A plumber in Malden should be talking about the triple deckers in Medford, the old pipe infrastructure in Somerville, the coastal weather coming off of Revere Beach. That's what makes Google… believe you're genuinely local" (`FifSqbB0:334-340`).
- **Supporting planner** (PAA + competitor headlines + Reddit → "topics beyond the core 30") is a REAL step too (`FifSqbB0:184-207`) — so research feeds supporting AND the Core-30 writing. Both true.
- **Geo/location pages are landmark-CENTRIC** (dedicated Geo Planner + Google Places API pulling neighborhoods/landmarks by distance from the GBP) — landmark-heavy, but NOT landmark-exclusive to geo (`FifSqbB0:244-314`).

**So the corrected answer:** competitor + local-landmark research is prescribed for **service/category/geo/supporting** alike; the per-type difference is the **writing prompt's emphasis**, not research gating. Landmarks are the *center* of geo pages but are also *woven into* service pages (real primary-city landmarks/conditions) for genuine local grounding.

## Q3 — Per-type workflow [CORRECTED]
| Type | Research fed pre-write | Local/landmark emphasis in the writing |
|---|---|---|
| **home** | competitor/differentiators; light local | primary-city, capability-led |
| **category** | competitor + local | overview + some local (older model even put neighborhoods here) |
| **service** | **competitor + local landmarks/conditions** (`FifSqbB0:414`) | service-depth in the primary city **woven with real local detail** — NOT landmark-free |
| **geo** *(after topical authority)* | **landmark-centric** (Geo Planner/Places → real landmarks) | the page is ABOUT the location; heavy real-landmark weave, published in waves |
| **supporting** | PAA + competitor + Reddit (the question) | answer depth; light local |

## Q4 — Reconcile with our tool [RECOMMENDATION REVERSED]
- **Build order: matches.** ✅
- **8-pass: `FifSqbB0:409-524` corroborates our S4-D decomposition exactly** (research-synthesis → outline → per-H2 section drafting → burstiness → perplexity/de-AI → human bookends → conversion → final QC, + parallel FAQ/meta/schema/images/external-links). Strong validation of S4-D.
- **Research UX — my earlier "restrict landmarks to geo / hide on service pages" recommendation was WRONG** per `FifSqbB0`. Our **current** S4-A (competitor + OSM-verified landmarks available on any page, fed to PROVIDED CONTEXT, gated by the reject-validator) is **the method-aligned design** — the expert's tool weaves real local landmarks into service pages too.
- **CORRECTED recommendation:** **Do NOT gate landmark research to geo.** Keep competitor + landmark research available on service/category/geo (primary-city real landmarks for service/category; the town's landmarks for geo). The per-type difference belongs in the **writing-prompt emphasis** (our §3 `typeBrief` already differentiates: geo = location-centric; service = service-depth), + the anti-hallucination guard + OSM verification for accuracy. The operator's instinct ("landmarks make no sense on a primary-city service page") is understandable but **not** what the expert's tool does — genuinely-local service content (real primary-city landmarks/conditions, verified) IS the method. The fix is **operator clarification + per-type prompt emphasis, NOT hiding the research.**
- *(Caveat: `pNPEz`/`How-to-Use-Content` are different models; if the operator ever wants the topical-first purity where service pages stay strictly service+city, that's the ZKZnDORR0ds emphasis — but even ZKZnDORR0ds wants service pages "specific… in this exact city," and FifSqbB0 (the tool) weaves local detail. Net: local grounding on service pages is in-method.)*

---
**Bottom line (corrected):** Build order = foundation → topical Core-30 (home→category→service) → publish → measure (rank map/top-3% vs threshold) → supporting/geo per the signal → maintain (ZKZnDORR0ds; our tool matches). **Research = competitor + local landmarks fed to ALL page types pre-write** (FifSqbB0), differing by writing-prompt emphasis, not gating. **Keep S4-A research available across page types (do not restrict landmarks to geo)** — that matches the expert tool; accuracy is handled by OSM-verify + the reject-validator. The 8-pass tool transcript validates the S4-D decomposition.
